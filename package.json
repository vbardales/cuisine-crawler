{
  "name": "cuisine-crawler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all -p nodemon browser-sync",
    "nodemon": "nodemon index.js",
    "browser-sync": "browser-sync start --proxy http://localhost:8080 --files public/**/*.* --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^18.4.0",
    "inert": "^5.1.3",
    "natural": "^0.6.3"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "nodemon": "^2.0.1",
    "npm-run-all": "^4.0.2"
  }
}
